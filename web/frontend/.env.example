VITE_API_URL=
# Leave blank for local dev (Vite's proxy handles /api -> localhost:4000).
# In production, set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://chms-backend.onrender.com
